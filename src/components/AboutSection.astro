---
// Componente para la sección 'Quiénes Somos'
---

<section id="nosotros" class="py-20 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
            <div class="fade-in-element">
                <h2 class="text-4xl font-bold text-gray-900 mb-6">Quiénes Somos</h2>
                <div class="section-divider w-20 ml-0"></div>
                <h3 class="text-2xl font-semibold text-primary mb-4">Su Aliado Estratégico en Ingeniería</h3>
                <p class="text-gray-600 mb-6 text-lg leading-relaxed">
                    Especialistas en automatización y soluciones tecnológicas, transformamos oportunidades en resultados estratégicos mediante una gestión integral de proyectos. Nos adaptamos con precisión a sus necesidades, abordando con confianza cualquier desafío que su visión demande.
                </p>
                <div class="bg-blue-50 border-l-4 border-primary p-6 rounded-r-lg">
                    <p class="text-gray-800 font-medium text-lg">
                        "Nuestra experiencia se demuestra en más de 100 casos de éxito y proyectos de ingeniería."
                    </p>
                </div>
            </div>
            
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 fade-in-element" style="transition-delay: 150ms;">
                <div class="bg-white rounded-xl shadow-lg p-6 card-hover">
                    <div class="text-center">
                        <i class="fas fa-award text-4xl text-primary mb-4"></i>
                        <h3 class="text-xl font-semibold text-gray-900 mb-2">Experiencia</h3>
                        <p class="text-gray-600">Más de 15 años en el mercado industrial.</p>
                    </div>
                </div>
                <div class="bg-white rounded-xl shadow-lg p-6 card-hover">
                    <div class="text-center">
                        <i class="fas fa-handshake text-4xl text-primary mb-4"></i>
                        <h3 class="text-xl font-semibold text-gray-900 mb-2">Confianza</h3>
                        <p class="text-gray-600">Líderes de la industria confían en nosotros.</p>
                    </div>
                </div>
                <div class="bg-white rounded-xl shadow-lg p-6 card-hover">
                    <div class="text-center">
                        <i class="fas fa-lightbulb text-4xl text-primary mb-4"></i>
                        <h3 class="text-xl font-semibold text-gray-900 mb-2">Innovación</h3>
                        <p class="text-gray-600">Tecnologías de vanguardia aplicadas.</p>
                    </div>
                </div>
                <div class="bg-white rounded-xl shadow-lg p-6 card-hover">
                    <div class="text-center">
                        <i class="fas fa-chart-line text-4xl text-primary mb-4"></i>
                        <h3 class="text-xl font-semibold text-gray-900 mb-2">Resultados</h3>
                        <p class="text-gray-600">Optimización comprobada de procesos.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .section-divider {
        height: 4px;
        background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
        margin: 2rem auto;
    }

    .card-hover { transition: all 0.3s ease; }
    .card-hover:hover {
        transform: translateY(-8px);
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.15);
    }
    
    /* --- ANIMACIONES DE SCROLL --- */
    .fade-in-element {
        opacity: 0;
        transform: translateY(30px);
        transition: opacity 0.6s ease-out, transform 0.6s ease-out;
    }

    .fade-in-element.is-visible {
        opacity: 1;
        transform: translateY(0);
    }
</style>

<script>
    // --- ANIMACIÓN DE SCROLL ---
    document.addEventListener('DOMContentLoaded', () => {
        const observer = new IntersectionObserver((entries, observerInstance) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('is-visible');
                    observerInstance.unobserve(entry.target);
                }
            });
        }, {
            threshold: 0.1 
        });

        const elementsToAnimate = document.querySelectorAll('.fade-in-element');
        elementsToAnimate.forEach(el => {
            observer.observe(el);
        });
    });
</script>